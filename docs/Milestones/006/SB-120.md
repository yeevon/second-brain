# SB-120 — Add daily digest workflow

**Branch:** `feature/daily-digest`

## Purpose

Run a daily digest on EC2 that reads the capture ledger (and optionally the vault) and posts a structured summary to `#brain-digest` or DM. Must work while the local PC is off.

---

## Implementation checklist

### Backend — capture-service API

- [x] `GET /internal/digest/daily` endpoint with shared-secret auth
- [x] `daily_digest_snapshot(since, now)` ledger query in `Ledger`
- [x] `DailyDigestResponse` Pydantic model
- [x] `new_captures_count` — captures received in last 24 h
- [x] `filed_notes_count` — `CAPTURE_FILED` events in last 24 h
- [x] `inbox_backlog_count` — current `INBOX` captures (point-in-time)
- [x] `awaiting_clarification_count` — current `clarification_status = NEEDS_CLARIFICATION`
- [x] `open_tasks_count` — vault scan via `scan_open_tasks()` (requires `VAULT_PATH`; returns `null` if unconfigured)
- [x] `failed_captures_count` — `RETRY_LIMIT_EXCEEDED` / `DELIVERY_FAILED_TERMINALLY` events in window
- [x] `retry_events_count` — `DELIVERY_RETRY_SCHEDULED` events in window
- [x] `sensitive_rejections_count` — count only, no content
- [x] `attachment_warnings_count` — captures with `has_attachments = 1` not yet in terminal status

### n8n workflow (`second-brain-daily-digest.json`)

- [x] Schedule trigger — 7 am UTC daily
- [x] `GET http://capture-service:8000/internal/digest/daily` with `PLACEHOLDER_CAPTURE_SERVICE_TOKEN` credential
- [x] Format Digest Message code node — renders all required sections:
  - [x] Intake (new captures, filed notes, sensitive rejections)
  - [x] Attachment warning line (conditional on count > 0)
  - [x] Backlog (inbox, awaiting clarification, open tasks)
  - [x] Failures & Retries (terminal failures, retry events)
- [x] Discord POST node
- [x] No `localhost` / `127.0.0.1` URLs
- [x] No restricted node types (no shell exec, SSH, etc.)
- [x] Credential IDs are `PLACEHOLDER_*`
- [x] Execution retention settings: no success/error/manual/progress saves
- [x] Fixture is `active: false`

### Architecture tests (`test_n8n_daily_digest_workflow.py`)

- [x] 16 tests — all passing

---

## Acceptance criteria (from ARCHITECTURE.md §Phase 5)

- [x] Digest workflows work while the local PC is off
- [x] Failed and unresolved captures remain visible
- [x] Rejected-sensitive captures report counts only, never sensitive content

---

## Deployment note

`open_tasks_count` requires `VAULT_PATH` to be set in the EC2 environment and the vault clone to exist. Without it the field is `null` and the Discord message omits the open-tasks line. No code change needed — configure the env var on EC2.
