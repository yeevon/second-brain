# SB-156 — Defensive SQLiteRuntime constructor validation

**Branch:** `fix/sqlite-runtime-constructor-validation`
**Source:** TD-P3-008

---

## Status: close as covered by SB-136

The `SQLiteRuntime` constructor in `src/secondbrain/sqlite_runtime.py` already validates its arguments defensively. The following checks are present (added during SB-136 watchdog work, marked `# TD-007`):

- `database_path` must not be `None` or empty string → raises `ValueError`
- `job_queue_maxsize` must be positive → raises `ValueError`
- `retry_attempts` must not be negative → raises `ValueError`
- SQLite version floor check → raises `RuntimeError`

All other constructor args (`busy_timeout_ms`, `startup_timeout_s`, `shutdown_timeout_s`, `queue_wait_timeout_s`, `job_completion_timeout_s`) are timeout values with sensible defaults. They are not validated because any positive integer is acceptable and a misconfigured timeout produces a clear failure at the first blocked operation rather than at construction.

**Resolution:** do not implement separately. The acceptance criteria are met.

---

## Evidence

```
src/secondbrain/sqlite_runtime.py lines 93-100:
    if database_path is None or str(database_path).strip() == "":
        raise ValueError("database_path must not be empty or None")
    if job_queue_maxsize <= 0:
        raise ValueError(f"job_queue_maxsize must be positive, got {job_queue_maxsize}")
    if retry_attempts < 0:
        raise ValueError(f"retry_attempts must not be negative, got {retry_attempts}")
```

---

## Acceptance criteria (confirmed met)

- [x] `database_path` empty or `None` raises `ValueError` at construction.
- [x] `job_queue_maxsize <= 0` raises `ValueError` at construction.
- [x] `retry_attempts < 0` raises `ValueError` at construction.
- [x] SQLite version below minimum raises `RuntimeError` at construction.
- [x] Covered by SB-136 — no separate implementation needed.

**Done.**
