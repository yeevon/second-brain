# SB-159 — Dockerized MCP profile ownership and safe.directory behavior

**Branch:** `fix/dockerized-mcp-ownership`
**Source:** TD-P3-011 / GitHub #16

---

## Goal

The Dockerized MCP container has Git `safe.directory` and vault ownership issues. These are low priority as long as the host-process `brain-mcp` remains the supported path.

> **Low priority if host-process `brain-mcp` is the supported MCP path.** Resolve or explicitly remove Dockerized MCP as a supported path.

---

## Acceptance criteria

One of the following:
- [ ] Fix Dockerized MCP vault ownership and `safe.directory` behavior so `git` commands succeed inside the container, OR
- [ ] Explicitly remove Dockerized MCP as a supported documented path and update `deploy/README.md` accordingly.

**Done when:** either Dockerized MCP works correctly or it is explicitly unsupported and documentation reflects that.
